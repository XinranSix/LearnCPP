#include <iostream>
#include <string>
#include <unordered_map>
#include <utility>
#include <vector>

// std::vector<std::string> words(std::string);

// template <typename C, typename T = typename C::value_type>
// std::unordered_map<T, unsigned int> count_occrrences(C const &collection);

// template <typename C, typename P1, typename P2>
// std::vector<std::pair<P2, P1>> reverse_pairs(C const &collection);

int main(int argc, char *argv[]) {
    return 0;
}
