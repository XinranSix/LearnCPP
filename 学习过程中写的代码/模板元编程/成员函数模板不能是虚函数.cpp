#include <iostream>

template<typename T>
struct Dynamic {
    virtual ~Dynamic();

    // template<typename T2>
    // virtual void copy(T2 const &);
};

int main(int arvc, char *argv[]) { return 0; }
