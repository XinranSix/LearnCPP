#include <stdio.h>

int main() {
    // int num;
    // char c;

    // printf("Enter a number: ");
    // scanf("%d", &num);

    // printf("Enter a character: ");
    // scanf(" %c", &c); // 注意前面的空格，用于消耗换行符

    // printf("Number: %d\n", num);
    // printf("Character: %c\n", c);

    fprintf(stdout, "hello");

    while (1)
        ;

    return 0;
}
