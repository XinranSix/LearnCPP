#include <stdio.h>

// void LOG(const char *fmt, ...) __attribute((format(printf, 1, 2)));

int main(int arvc, char *argv[]) {
    // LOG("I am litao\n");
    // LOG("I am litao, I have %d houses!\n", 0);
    // LOG("I am litao, I have %d housed! %d cars\n", 0, 0);
    return 0;
}
